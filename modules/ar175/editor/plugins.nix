{
  perSystem = {
    myNvf.settings = {
      git.gitsigns.enable = true;

      utility.yanky-nvim.setupOpts.highlight.on_yank = true;

      notes.todo-comments.enable = true;

      session.persisted.enable = true;

      presence.cord-nvim.enable = true;

      mini = {
        ai.enable = true;
        pairs.enable = true;
        surround.enable = true;
        icons = {
          enable = true;
          setupOpts.mock_nvim_web_devicons = true;
        };
      };
    };
  };
}
